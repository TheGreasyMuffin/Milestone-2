let player;

function Player(classType, health, speed, magic, stamina) {
    this.classType = classType;
    this.health = health;
    this.speed = speed;
    this.magic = magic; 
    this.stamina = stamina;
}